set -g @thm_fg "#4c4f69"
# Explicit base colour rather than "default" — see catppuccin-mocha.conf.
set -g @thm_bg "#eff1f5"
set -g @surface0 "#ccd0da"
set -g @surface1 "#bcc0cc"
set -g @teal "#179299"
set -g @blue "#1e66f5"
set -g @mauve "#8839ef"
# Mauve blended into the base — see catppuccin-mocha.conf.
set -g @mauve_dim "#cbb1f3"
